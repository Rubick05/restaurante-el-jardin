import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.restaurante.pelusa',
    appName: 'Restaurante Pelusa',
    webDir: 'dist',
    server: {
        // androidScheme: 'https'
    },
    plugins: {
        // Bluetooth plugin configuration would go here
    }
};

export default config;
